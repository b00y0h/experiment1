---
phase: 02-block-types
plan: 02
type: execute
---

<objective>
Implement FAQ and Stats block types for the Page collection content section.

Purpose: Complete the four core block types by adding FAQ for Q&A content and Stats for numeric displays.
Output: Working FAQ block (question/answer pairs) and Stats block (numeric values with labels) in Pages collection.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-block-types/02-01-SUMMARY.md
@dev/collections/Pages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FAQ block to content section</name>
  <files>dev/collections/Pages.ts</files>
  <action>
Add faqBlock to the content section's blocks array:
- slug: 'faqBlock'
- items: array field containing:
  - question: text field, required
  - answer: richText field with lexicalEditor() (allows formatted answers)

The FAQ block is distinct from Accordion - it's semantically for Q&A content which may have different frontend rendering and SEO implications (FAQ schema markup).
  </action>
  <verify>pnpm generate:types succeeds without errors</verify>
  <done>FAQ block available in content section with items array containing question and answer fields</done>
</task>

<task type="auto">
  <name>Task 2: Add Stats block to content section</name>
  <files>dev/collections/Pages.ts</files>
  <action>
Add statsBlock to the content section's blocks array:
- slug: 'statsBlock'
- stats: array field containing:
  - value: text field, required (e.g., "99%", "10M+", "$50K")
  - label: text field, required (e.g., "Uptime", "Users", "Saved")
  - icon: text field, optional (icon name or emoji for frontend to render)

Use text for value instead of number to allow formatted strings like "99.9%" or "10M+".
  </action>
  <verify>pnpm generate:types succeeds and pnpm lint passes</verify>
  <done>Stats block available in content section with stats array containing value, label, and optional icon fields</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm generate:types` succeeds without errors
- [ ] `pnpm lint` passes
- [ ] FAQ block has items array with question and answer fields
- [ ] Stats block has stats array with value, label, and icon fields
- [ ] All four block types (Hero, Accordion, FAQ, Stats) appear in generated payload-types.ts
- [ ] Content section now has 4 block options (contentBlock, accordionBlock, faqBlock, statsBlock)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Phase 2 complete: All four core block types implemented
</success_criteria>

<output>
After completion, create `.planning/phases/02-block-types/02-02-SUMMARY.md`
</output>
